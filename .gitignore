# Real Navidrome credentials + your specific Bluetooth MAC live here -
# config/mopidy.conf.example is the committed template.
config/mopidy.conf

# OS / editor junk
.DS_Store
__pycache__/
*.pyc

# Claude Code session state, not part of the project
.claude/

# Frontend build artifacts and dependencies - built with `pnpm build` in
# frontend/, then deployed to the Pi separately (see README.md)
frontend/node_modules/
frontend/dist/
